[tool.poetry]
name = "discovery"
version = "0.1.0"
description = "Discovery app"
authors = ["Daniel Vopalecky"]

[tool.poetry.dependencies]
python = "^3.8"
mkdocs = "^1.2.3"
mkdocs-material = "^8.1.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
